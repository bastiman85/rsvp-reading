services:
  rsvp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - rsvp-data:/app/data
    restart: unless-stopped

volumes:
  rsvp-data:
